#name: Build & Deploy Docker to AWS EC2
#
#on:
#  workflow_dispatch:        # Manual trigger (click button in GitHub)
#  push:
#    branches: [main]        # Auto trigger on push to main
#
#jobs:
#  build-and-deploy:
#    runs-on: ubuntu-latest
#
#    steps:
#      # Step 1: Checkout the code
#      - name: Checkout Code
#        uses: actions/checkout@v3
#
#      # Step 2: Log in to Docker Hub using username + access token
#      - name: Login to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}    # ← this is your access token
#
#      # Step 3: Build the Docker image
#      - name: Build Docker Image
#        run: |
#          docker build -t ${{ secrets.DOCKER_USERNAME }}/my-app:latest .
#
#      # Step 4: Push the image to Docker Hub
#      - name: Push Docker Image to Docker Hub
#        run: |
#          docker push ${{ secrets.DOCKER_USERNAME }}/my-app:latest
#
#      # Step 5: SSH into EC2 and deploy the new image
#      - name: Deploy to EC2
#        uses: appleboy/ssh-action@v1.0.0
#        with:
#          host: ${{ secrets.EC2_HOST }}
#          username: ${{ secrets.EC2_USER }}
#          key: ${{ secrets.EC2_SSH_KEY }}
#          script: |
#            # Pull the latest image from Docker Hub
#            docker pull ${{ secrets.DOCKER_USERNAME }}/my-app:latest
#
#            # Stop and remove the old running container if exists
#            docker stop my-app || true
#            docker rm my-app || true
#
#            # Run the new container
#            docker run -d \
#              --name my-app \
#              --restart always \
#              -p 80:80 \
#              ${{ secrets.DOCKER_USERNAME }}/my-app:latest
#
#            # Verify container is running
#            docker ps | grep my-app
